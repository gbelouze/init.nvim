return {
    s({
        trig = ";a",
        snippetType = "autosnippet",
        wordTrig = false,
    }, {
        t("\\alpha"),
    }, { condition = in_mathzone }),
    s({
        trig = ";b",
        snippetType = "autosnippet",
        wordTrig = false,
    }, {
        t("\\beta"),
    }, { condition = in_mathzone }),
    s({
        trig = ";g",
        snippetType = "autosnippet",
        wordtrig = false,
    }, {
        t("\\gamma"),
    }, { condition = in_mathzone }),
    s({
        trig = ";d",
        snippetType = "autosnippet",
        wordtrig = false,
    }, {
        t("\\delta"),
    }, { condition = in_mathzone }),
    s({
        trig = ";p",
        snippetType = "autosnippet",
        wordtrig = false,
    }, {
        t("\\psi"),
    }, { condition = in_mathzone }),
    s({
        trig = ";f",
        snippetType = "autosnippet",
        wordtrig = false,
    }, {
        t("\\phi"),
    }, { condition = in_mathzone }),
    s({
        trig = ";m",
        snippetType = "autosnippet",
        wordtrig = false,
    }, {
        t("\\mu"),
    }, { condition = in_mathzone }),
    s({
        trig = ";n",
        snippetType = "autosnippet",
        wordtrig = false,
    }, {
        t("\\nu"),
    }, { condition = in_mathzone }),
}
